# Fixture env file. In a real project this would be gitignored; it's committed here
# because the fixture needs deterministic input. CI rewrites it to prove that editing
# the env file re-triggers generation without a clean build.
export TIGHTLIP_DEMO_KEY=from-env-file
